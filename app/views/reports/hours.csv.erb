last_name,first_name,grade,email,student_id,hours
<% for member in @members do -%>
<%= member.last_name %>, <%= member.first_name %>, <%= member.grade %>, <%= member.email %>, <%= member.student_id %>, <%= member.hours %>
<% end %>
