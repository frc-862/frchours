- content_for :navbar, render("layouts/user_nav")

= render :layout => "layouts/base" do
  .page-header
    .row
      .col-md-7
        %h2= @title
        = yield :description
      .col-md-2
        = yield :actions

  .row
    #content.col-md-9
      = yield

    #sidebar.col-md-3.live-info
      %h6 Live content data
      %p
        foo
      %p
        Test stuff
        wow

      -#%ul.nav.nav-tabs.nav-stacked.nav-caret-left
        -#= activatable_li_tag_with_link "Sub menu", "#"
